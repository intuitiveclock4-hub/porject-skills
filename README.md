# School Wellbeing - Static Site

This is a full static HTML/CSS/JS export of the School Wellbeing project.

Files:
- `index.html` — main page
- `style.css` — styling
- `script.js` — small client-side interactions

To deploy: upload these files to the root of a GitHub repository and enable GitHub Pages (branch `main`, folder `/`).
